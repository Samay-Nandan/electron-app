name: Build and Release

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        include:
          - os: ubuntu-latest
            build_script: build:linux
            artifact_paths: |
              build/linux/**/*.AppImage
              build/linux/**/*.deb
          - os: windows-latest
            build_script: build:win
            artifact_paths: |
              build/windows/**/*.exe
              build/windows/**/*.zip

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "yarn"

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build full app (${{ matrix.os }})
        run: yarn ${{ matrix.build_script }}
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Check for server.exe (Windows only)
        if: matrix.os == 'windows-latest'
        run: |
          echo "🔍 Checking for server.exe at: build/windows/server/server.exe"
          ls -lh build/windows/server || echo "⚠️ build/windows/server does not exist"
          find build/windows -name server.exe || echo "❌ server.exe not found"

      - name: Upload artifacts to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.artifact_paths }}
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
